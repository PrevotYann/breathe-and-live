{
  "name": "breathe-and-live",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "rimraf dist && vite build",
    "clean:packs": "node resources/scripts/clean-compendiums.mjs",
    "build:packs": "node resources/scripts/compile-packs.mjs",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@foundryvtt/foundryvtt-cli": "^1.0.3",
    "vite": "^5.4.0",
    "typescript": "^5.5.4",
    "esbuild": "^0.23.0",
    "rimraf": "^5.0.5",
    "eslint": "^9.9.0"
  }
}
