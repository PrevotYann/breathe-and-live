{
  "name": "breathe-and-live",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "rimraf dist && vite build",
    "lint": "eslint ."
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "typescript": "^5.5.4",
    "esbuild": "^0.23.0",
    "rimraf": "^5.0.5",
    "eslint": "^9.9.0"
  }
}